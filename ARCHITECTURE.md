# 🏗️ Architecture & Flow Diagrams

This document explains the architecture and data flow of the application.

---

## 📐 Project Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         BROWSER                                  │
│                   http://localhost                               │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ HTTP Request
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                 NGINX CONTAINER (Port 80)                        │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Location: /                                              │  │
│  │  Action: Serve static files (HTML, CSS, JS)              │  │
│  │  Files: /usr/share/nginx/html/*                          │  │
│  └──────────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Location: /api/*                                         │  │
│  │  Action: Proxy to backend                                │  │
│  │  Target: http://backend:4000                             │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────┬───────────────────────────┬────────────────────────┘
             │                           │
             │ Static Files              │ API Requests
             ▼                           ▼
┌─────────────────────┐    ┌──────────────────────────────────────┐
│   REACT APP         │    │   EXPRESS CONTAINER (Port 4000)      │
│   (Client-side)     │    │  ┌─────────────────────────────────┐ │
│   - Components      │    │  │  Route: GET /api/message        │ │
│   - State           │    │  │  Handler: Express router        │ │
│   - UI Logic        │    │  │  Response: JSON data            │ │
│   - Fetch API       │    │  └─────────────────────────────────┘ │
└─────────────────────┘    └──────────────────────────────────────┘
```

---

## 🔄 Request Flow - Development vs Production

### Development Mode (No Docker)

```
┌──────────┐
│ BROWSER  │
└────┬─────┘
     │
     │ http://localhost:3000
     ▼
┌─────────────────────┐
│  VITE DEV SERVER    │ ← Hot Module Replacement (HMR)
│  (Port 3000)        │
└────┬────────────┬───┘
     │            │
     │ /          │ /api/* → proxy
     │            │
     ▼            ▼
┌─────────┐  ┌──────────────┐
│  React  │  │   EXPRESS    │
│  Files  │  │  (Port 4000) │
└─────────┘  └──────────────┘
```

**Key Points:**
- Vite serves React files directly
- Vite proxies `/api/*` requests to Express
- No CORS issues (same origin via proxy)
- Hot Module Replacement for instant updates

---

### Production Mode (Docker)

```
┌──────────┐
│ BROWSER  │
└────┬─────┘
     │
     │ http://localhost:80
     ▼
┌─────────────────────────────────┐
│    NGINX CONTAINER              │
│    (Frontend + Reverse Proxy)   │
└────┬────────────┬───────────────┘
     │            │
     │ /          │ /api/*
     │            │
     ▼            ▼
┌─────────┐  ┌────────────────────┐
│ Static  │  │  EXPRESS CONTAINER │
│  React  │  │   (Backend API)    │
│  Files  │  │                    │
└─────────┘  └────────────────────┘
     │                  │
     │                  │
     └──────┬───────────┘
            │
    Docker Network
  (Services communicate
   by service name)
```

**Key Points:**
- Nginx serves pre-built React files
- Nginx proxies API requests to backend
- No CORS issues (same origin)
- Services communicate via Docker network
- Production-optimized (gzip, caching, etc.)

---

## 📊 Data Flow - Frontend API Call

### Step-by-Step Breakdown

```
1. Component Mount
   ┌─────────────────┐
   │   App.jsx       │
   │  useEffect()    │ → Runs after component renders
   └────────┬────────┘
            │
            ▼
2. API Call
   ┌─────────────────────────────────┐
   │  fetch("/api/message")          │ → Browser sends request
   └────────┬────────────────────────┘
            │
            ▼
3. Request Routing (Production)
   ┌─────────────────────────────────┐
   │  Nginx receives request         │
   │  Matches location /api/         │
   │  Proxies to backend:4000        │
   └────────┬────────────────────────┘
            │
            ▼
4. Backend Processing
   ┌─────────────────────────────────┐
   │  Express receives request       │
   │  Matches route GET /api/message │
   │  Executes handler function      │
   └────────┬────────────────────────┘
            │
            ▼
5. Response Generation
   ┌─────────────────────────────────┐
   │  res.json({                     │
   │    message: "Hello..."          │
   │  })                             │
   └────────┬────────────────────────┘
            │
            ▼
6. Response Routing
   ┌─────────────────────────────────┐
   │  Backend → Nginx → Browser      │
   └────────┬────────────────────────┘
            │
            ▼
7. Frontend Processing
   ┌─────────────────────────────────┐
   │  .then(res => res.json())       │ → Parse JSON
   │  .then(data => setMessage(...)) │ → Update state
   └────────┬────────────────────────┘
            │
            ▼
8. UI Update
   ┌─────────────────────────────────┐
   │  React re-renders component     │
   │  Displays updated message       │
   └─────────────────────────────────┘
```

---

## 🐳 Docker Architecture

### Multi-Stage Build (Frontend)

```
Stage 1: BUILD
┌────────────────────────────────┐
│  FROM node:22-alpine AS build  │
├────────────────────────────────┤
│  WORKDIR /app                  │
│  COPY package*.json ./         │
│  RUN npm install               │
│  COPY . .                      │
│  RUN npm run build             │
│                                │
│  Result: /app/dist/            │
│  - index.html                  │
│  - assets/*.js                 │
│  - assets/*.css                │
└────────────────────────────────┘
            │
            │ Only copy dist/
            ▼
Stage 2: PRODUCTION
┌────────────────────────────────┐
│  FROM nginx:stable-alpine      │
├────────────────────────────────┤
│  COPY --from=build /app/dist   │
│       /usr/share/nginx/html    │
│                                │
│  COPY nginx.conf               │
│       /etc/nginx/conf.d/       │
│                                │
│  CMD ["nginx", "-g",           │
│       "daemon off;"]           │
└────────────────────────────────┘
            │
            ▼
Final Image: ~27MB
(vs ~500MB if we kept Node.js)
```

---

## 🔌 Network Communication

### Docker Compose Network

```
Docker Compose creates a network: react-express-docker_default

┌────────────────────────────────────────────────────────┐
│                  DOCKER NETWORK                        │
│                                                        │
│  ┌──────────────────┐      ┌───────────────────────┐ │
│  │  Frontend        │      │  Backend              │ │
│  │  Service: client │      │  Service: backend     │ │
│  │  Hostname: client│      │  Hostname: backend    │ │
│  │  Port: 80        │◄────►│  Port: 4000           │ │
│  └──────────────────┘      └───────────────────────┘ │
│         │                           │                │
└─────────┼───────────────────────────┼────────────────┘
          │                           │
          │ Port Mapping              │ Port Mapping
          ▼                           ▼
    Host Port 80              Host Port 4000
```

**Key Concepts:**

1. **Internal Communication**
   - Frontend → Backend: `http://backend:4000`
   - Uses Docker's internal DNS
   - Fast, no need to go through host

2. **External Access**
   - Host → Frontend: `http://localhost:80`
   - Host → Backend: `http://localhost:4000`
   - Port mapping in docker-compose.yml

3. **Service Discovery**
   - Docker provides DNS resolution
   - Service name = hostname
   - `backend` resolves to backend container's IP

---

## 📦 Build Process

### Frontend Build Pipeline

```
Source Code → Vite Build → Production Bundle
───────────────────────────────────────────

1. Entry Point
   index.html
   └─ <script src="/src/main.jsx">

2. Module Resolution
   main.jsx
   └─ import App from './App'
      └─ App.jsx
         └─ import React, { useState, useEffect }

3. Compilation
   JSX → JavaScript
   ES6+ → ES5 (if needed)
   Process imports
   Bundle modules

4. Optimization
   Tree shaking (remove unused code)
   Code splitting (separate chunks)
   Minification (compress code)
   Hashing filenames (cache busting)

5. Output (dist/)
   index.html
   assets/index-[hash].js    ← Main bundle
   assets/index-[hash].css   ← Styles
   assets/vendor-[hash].js   ← Third-party libraries
```

---

## 🔐 Security Layers

```
┌──────────────────────────────────────────────────┐
│  External Request                                │
└────────────┬─────────────────────────────────────┘
             │
             ▼
┌──────────────────────────────────────────────────┐
│  Layer 1: Docker Host Firewall                  │
│  - Controls external access                     │
└────────────┬─────────────────────────────────────┘
             │
             ▼
┌──────────────────────────────────────────────────┐
│  Layer 2: Docker Network                        │
│  - Isolates containers                          │
│  - Only mapped ports accessible from host       │
└────────────┬─────────────────────────────────────┘
             │
             ▼
┌──────────────────────────────────────────────────┐
│  Layer 3: Nginx                                 │
│  - Rate limiting                                │
│  - Request filtering                            │
│  - Security headers                             │
└────────────┬─────────────────────────────────────┘
             │
             ▼
┌──────────────────────────────────────────────────┐
│  Layer 4: Application (Express)                 │
│  - Input validation                             │
│  - Authentication                               │
│  - Authorization                                │
└──────────────────────────────────────────────────┘
```

---

## 🚀 Performance Optimization

### Image Size Optimization

```
Without Multi-Stage Build:
├─ Node.js base image: 150 MB
├─ npm dependencies: 200 MB
├─ Source code: 5 MB
├─ Built files: 2 MB
└─ Total: ~357 MB

With Multi-Stage Build:
├─ Nginx base image: 25 MB
├─ Built files: 2 MB
└─ Total: ~27 MB

Savings: 330 MB (92% reduction!)
```

### Network Performance

```
Without Gzip:
├─ bundle.js: 500 KB
├─ styles.css: 100 KB
└─ Total: 600 KB

With Gzip:
├─ bundle.js: 100 KB (80% reduction)
├─ styles.css: 30 KB (70% reduction)
└─ Total: 130 KB

Savings: 470 KB (78% reduction!)
Transfer time on 3G: 15s → 3s
```

---

## 📈 Scaling Strategy

### Horizontal Scaling

```
Load Balancer
     │
     ├─► Frontend Container 1 ─┐
     ├─► Frontend Container 2 ─┼─► Backend Container 1
     ├─► Frontend Container 3 ─┤    Backend Container 2
     └─► Frontend Container 4 ─┘    Backend Container 3
```

To scale with Docker Compose:
```bash
docker compose up -d --scale client=3 --scale backend=2
```

---

This architecture provides:
✅ **Separation of Concerns** - Frontend and backend are independent
✅ **Scalability** - Can scale services independently
✅ **Performance** - Optimized builds and delivery
✅ **Security** - Multiple layers of protection
✅ **Maintainability** - Clear structure and documentation
