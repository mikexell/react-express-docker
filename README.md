# react-express-docker
A complete full-stack example using React (Vite) and Express.js, containerized with Docker and orchestrated using Docker Compose. Demonstrates frontend-backend communication with CORS, reverse proxy (nginx), and easy local or production deployment.
